query Location($locationId: ID!) {
    location(id: $locationId) {
        id
        name
        type
        dimension
        created
        residents {
            name
            status
            gender
            image
            type
        }
    }
}